"use client";
import { useState } from "react";
import { supabase } from "@/lib/supabase";
export default function CorrectionsPage(){const[form,setForm]=useState({name:"",email:"",story_url:"",details:""});const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMsg("Sending...");const{error}=await supabase.from("correction_requests").insert({...form,status:"new"});setMsg(error?error.message:"Thanks — the editor will review it.");}return <main className="mx-auto max-w-3xl px-4 py-10"><p className="text-sm font-black uppercase tracking-widest text-hgnBlue">Accuracy</p><h1 className="mt-2 text-5xl font-black text-hgnNavy">Corrections & clarifications</h1><p className="mt-4 leading-7 text-slate-700">See something that needs correcting? Send the newsroom a clear note and a link to the story.</p><form onSubmit={submit} className="hgn-card mt-8 space-y-4 p-6"><input className="hgn-input" placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="hgn-input" placeholder="Email" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="hgn-input" placeholder="Story URL or headline" value={form.story_url} onChange={e=>setForm({...form,story_url:e.target.value})}/><textarea className="hgn-input min-h-40" placeholder="What should we review?" value={form.details} onChange={e=>setForm({...form,details:e.target.value})}/><button className="hgn-btn-primary">Submit correction</button>{msg&&<p className="font-semibold text-hgnNavy">{msg}</p>}</form></main>}
