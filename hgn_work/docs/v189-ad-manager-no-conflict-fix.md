# HGN v189 — Ad Manager SQL Fix

This fixes the ad manager SQL failure:

`there is no unique or exclusion constraint matching the conflict specification`

## SQL to run

`supabase/v189-ad-manager-no-conflict-fix.sql`

You do not need to rerun `v188-ad-manager.sql`.
