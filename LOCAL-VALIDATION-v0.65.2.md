# Local validation status

This package is labelled LOCAL-VALIDATION. Full `npm install`, `npm run typecheck`, and `npm run build` were not completed in the packaging environment. Run all three locally before deployment.
