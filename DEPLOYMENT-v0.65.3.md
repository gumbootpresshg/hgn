# Deployment — v0.65.3
No Supabase migration is required. Overlay the release while preserving `.git`, `.env.local`, `node_modules`, and `.next`; then run npm.cmd install, typecheck, build, commit, and push main.
