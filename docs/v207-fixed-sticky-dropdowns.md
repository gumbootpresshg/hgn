# HGN v207 — Fixed Sticky Nav + Dropdown Hover Fix

## Fixed

- Sticky menu now uses scroll detection and `position: fixed`.
- The small “Haida Gwaii News” wordmark only appears once the masthead has scrolled out of view.
- Dropdowns no longer disappear when moving from the menu label to the dropdown.
- Removed the gap between trigger and dropdown by using padding on the hover group instead of margin.

## SQL

No SQL required.
