# HGN v215 — Clean Columnists SQL

Fixes the SQL paste error:

`syntax error at or near "e"`

## What to do

1. Open Supabase SQL Editor.
2. Clear the editor completely.
3. Paste only this file:
   `supabase/v215-clean-columnists-schema.sql`
4. Run it.
5. Refresh `/admin/columns`.

This replaces the broken v213 paste.
