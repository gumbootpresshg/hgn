# HGN Website Build v45 — Beta Hardening

Unzip this over `C:\HGN\HGNSite` and replace files.

## After replacing
```powershell
npm install
Remove-Item .next -Recurse -Force
npm run dev
```

## Supabase
Run `supabase/v45-upgrade.sql`.

Focus: beta launch checklist, support page polish, advertiser page polish, event calendar polish, public CTA cleanup, admin launch readiness tools.
