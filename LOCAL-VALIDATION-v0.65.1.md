# Local validation status — v0.65.1

Validation must include `npm run typecheck` and `npm run build` before production deployment. This package should be treated as LOCAL-VALIDATION unless both commands complete successfully in the target environment.
