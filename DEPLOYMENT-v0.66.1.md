# Deploy HGN v0.66.1

Run `supabase/v293-newsletter-editorial-control.sql` in Public HGN Supabase after the v291 and corrected v292 migrations. Then overlay the package, run `npm.cmd install`, `npm.cmd run typecheck`, and `npm.cmd run build` before pushing `main`.
