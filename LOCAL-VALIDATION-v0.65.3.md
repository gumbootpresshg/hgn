# Local Validation — v0.65.3
Source was built from the supplied v0.65.2 release. The environment did not contain the project's installed dependency tree, so full Next.js typecheck/build could not be completed here. Run `npm.cmd install`, `npm.cmd run typecheck`, and `npm.cmd run build` locally before push. No migration required.
