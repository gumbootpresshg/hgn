# Local validation status

`npm run typecheck` passed in the build workspace.

`npm run build` needs to be run on the deployment computer. The hosted execution runner stopped the Next/Turbopack build process before completion, so this release is intentionally marked `LOCAL-VALIDATION` even though TypeScript validation passed.
