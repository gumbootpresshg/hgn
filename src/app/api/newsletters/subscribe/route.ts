import { NextRequest, NextResponse } from "next/server";
import { serviceClient } from "@/lib/newsletters/server";
export const runtime="nodejs";
export async function POST(req:NextRequest){const b=await req.json().catch(()=>({}));const email=String(b.email||"").trim().toLowerCase();if(!/^\S+@\S+\.\S+$/.test(email))return NextResponse.json({error:"Enter a valid email address."},{status:400});const db=serviceClient();const {data:products}=await db.from("hgn_newsletter_products").select("slug").eq("status","active").eq("show_on_public_signup",true).eq("accept_new_subscribers",true);const allowed=new Set((products||[]).map((x:any)=>x.slug));const selected=Array.isArray(b.products)?b.products.map(String).filter((x:string)=>allowed.has(x)).slice(0,20):[];const {data:existing}=await db.from("subscribers").select("id,newsletter_product_slugs").ilike("email",email).maybeSingle();const row={email,name:String(b.name||"").trim()||null,town:String(b.town||"").trim()||null,status:"active",frequency:"biweekly",consent_source:"newsletter_page",newsletter_product_slugs:selected.length?selected:(existing?.newsletter_product_slugs||["hgn-news"]),updated_at:new Date().toISOString()};const result=existing?.id?await db.from("subscribers").update(row).eq("id",existing.id):await db.from("subscribers").insert(row);return result.error?NextResponse.json({error:result.error.message},{status:500}):NextResponse.json({ok:true});}
