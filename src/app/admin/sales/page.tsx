"use client";import {FormEvent,useEffect,useState} from "react";import {supabase} from "@/lib/supabase";type L={id:string;business_name:string;contact_name:string|null;email:string|null;phone:string|null;stage:string;estimated_value:number|null;last_contact_at:string|null;next_follow_up:string|null;source:string|null;notes:string|null};const stages=["new","contacted","interested","quote_sent","booked","not_now","closed"];
export default function SalesPage(){const[rows,setRows]=useState<L[]>([]),[msg,setMsg]=useState("");async function load(){const{data,error}=await supabase.from("hgn_sales_leads").select("*").order("next_follow_up",{ascending:true,nullsFirst:false});setRows((data||[])as L[]);setMsg(error?.message||"")}useEffect(()=>{void load()},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const{error}=await supabase.from("hgn_sales_leads").insert({business_name:f.get("business_name"),contact_name:f.get("contact_name")||null,email:f.get("email")||null,phone:f.get("phone")||null,source:f.get("source")||null,estimated_value:f.get("estimated_value")?Number(f.get("estimated_value")):null,next_follow_up:f.get("next_follow_up")||null,notes:f.get("notes")||null});if(error)setMsg(error.message);else{e.currentTarget.reset();void load()}}async function stage(id:string,value:string){const{error}=await supabase.from("hgn_sales_leads").update({stage:value,last_contact_at:["contacted","interested","quote_sent"].includes(value)?new Date().toISOString():undefined,updated_at:new Date().toISOString()}).eq("id",id);if(error)setMsg(error.message);else void load()}return <main className="mx-auto max-w-7xl space-y-7 px-6 py-10"><header className="rounded-3xl border bg-white p-8"><p className="text-sm font-black uppercase tracking-widest text-hgnBlue">Advertising growth</p><h1 className="mt-2 font-serif text-5xl font-bold">Sales Desk</h1><p className="mt-2 text-slate-600">A lightweight follow-up list for leads, renewals and advertisers worth contacting again.</p></header>{msg&&<p className="rounded-xl border bg-white p-4 text-red-800">{msg}</p>}<section className="grid gap-6 lg:grid-cols-[1fr_2fr]"><form onSubmit={add} className="grid content-start gap-3 rounded-3xl border bg-white p-6"><h2 className="font-serif text-3xl font-bold">Add lead</h2><input required name="business_name" placeholder="Business or organization"/><input name="contact_name" placeholder="Contact name"/><input name="email" type="email" placeholder="Email"/><input name="phone" placeholder="Phone"/><input name="source" placeholder="How we found them"/><input name="estimated_value" type="number" step="0.01" placeholder="Estimated value"/><label className="grid gap-1 font-bold">Next follow-up<input name="next_follow_up" type="date"/></label><textarea name="notes" rows={4} placeholder="What they may need, seasonal timing, prior contact"/><button className="hgn-btn-primary">Save lead</button></form><section className="grid gap-4">{rows.map(r=><article key={r.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-serif text-2xl font-bold">{r.business_name}</h2><p className="text-sm text-slate-600">{r.contact_name||"No contact"} · {r.email||r.phone||"No contact details"}</p></div>{r.estimated_value!==null&&<strong>${Number(r.estimated_value).toFixed(2)}</strong>}</div><div className="mt-3 flex flex-wrap gap-3 text-sm"><span>Stage: <strong>{r.stage.replaceAll("_"," ")}</strong></span><span>Follow-up: <strong>{r.next_follow_up||"Not set"}</strong></span>{r.source&&<span>Source: {r.source}</span>}</div>{r.notes&&<p className="mt-3 whitespace-pre-wrap text-sm text-slate-600">{r.notes}</p>}<div className="mt-4 flex flex-wrap gap-2">{stages.map(s=><button key={s} onClick={()=>void stage(r.id,s)} className={`rounded-full border px-3 py-2 text-xs font-black ${r.stage===s?"bg-slate-950 text-white":""}`}>{s.replaceAll("_"," ")}</button>)}</div></article>)}{!rows.length&&<p className="rounded-2xl border border-dashed p-8 text-center text-slate-500">No sales leads yet.</p>}</section></section></main>}
