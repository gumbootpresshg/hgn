"use client";
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
export default function AdminSubscribersPage(){const[rows,setRows]=useState<any[]>([]);const[msg,setMsg]=useState("");useEffect(()=>{load()},[]);async function load(){const{data,error}=await supabase.from("audience_members").select("*").order("created_at",{ascending:false});if(error)setMsg(error.message);setRows(data||[])}function csv(){const header=["name","email","plan","status","source","created_at"];const body=rows.map(r=>header.map(h=>`"${String(r[h]??"").replaceAll('"','""')}"`).join(","));const blob=new Blob([[header.join(","),...body].join("")],{type:"text/csv"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="hgn-audience.csv";a.click();URL.revokeObjectURL(url)}return <main className="mx-auto max-w-6xl px-4 py-10"><div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-black uppercase tracking-widest text-hgnBlue">Admin</p><h1 className="mt-2 text-5xl font-black text-hgnNavy">Audience list</h1><p className="mt-2 text-slate-600">Newsletter, digital paper and supporter interest.</p></div><button onClick={csv} className="hgn-btn-primary">Export CSV</button></div>{msg&&<p className="mt-4 font-semibold text-hgnBlue">{msg}</p>}<div className="hgn-card mt-8 overflow-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr>{["Name","Email","Plan","Status","Source","Created"].map(h=><th key={h} className="p-3">{h}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t"><td className="p-3">{r.name}</td><td className="p-3">{r.email}</td><td className="p-3">{r.plan}</td><td className="p-3">{r.status}</td><td className="p-3">{r.source}</td><td className="p-3">{r.created_at?new Date(r.created_at).toLocaleString():""}</td></tr>)}</tbody></table>{!rows.length&&<div className="p-6 text-slate-600">No subscribers yet.</div>}</div></main>}
