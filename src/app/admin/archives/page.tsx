"use client"
import { FormEvent,useEffect,useState } from "react"
import { Archive,Trash2,Upload } from "lucide-react"
import { supabase } from "@/lib/supabase"
async function authHeaders():Promise<Record<string,string>>{const{data}=await supabase.auth.getSession();return data.session?.access_token?{Authorization:`Bearer ${data.session.access_token}`}:{}}
export default function ArchivesAdmin(){const[items,setItems]=useState<any[]>([]),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function load(){const r=await fetch('/api/admin/archives',{headers:await authHeaders(),cache:'no-store'});const j=await r.json();if(r.ok)setItems(j.editions||[]);else setMsg(j.error||'Could not load editions.')}useEffect(()=>{void load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget);const r=await fetch('/api/admin/archives',{method:'POST',headers:await authHeaders(),body:form});const j=await r.json();setMsg(r.ok?'Edition saved to the newsstand.':j.error||'Could not save edition.');if(r.ok){e.currentTarget.reset();await load()}setBusy(false)}async function patch(id:string,body:any){const r=await fetch(`/api/admin/archives/${id}`,{method:'PATCH',headers:{...(await authHeaders()),'Content-Type':'application/json'},body:JSON.stringify(body)});if(r.ok)await load()}async function del(id:string){if(!confirm('Delete this archive record?'))return;const r=await fetch(`/api/admin/archives/${id}`,{method:'DELETE',headers:await authHeaders()});if(r.ok)await load()}
return <main className="mx-auto max-w-7xl space-y-6 px-4 py-8 sm:px-6"><section className="rounded-3xl border bg-white p-8 shadow-sm"><p className="text-sm font-black uppercase tracking-[.18em] text-hgnBlue">Archives</p><h1 className="mt-2 font-serif text-5xl font-bold">Newsstand manager</h1><p className="mt-3 max-w-3xl text-slate-600">Upload print editions, cover images and PDFs. Published editions appear on the public virtual newsstand.</p></section>{msg&&<div className="rounded-2xl border bg-white p-4 font-bold">{msg}</div>}<form onSubmit={submit} className="grid gap-4 rounded-3xl border bg-white p-6 shadow-sm md:grid-cols-2"><label>Edition title<input name="title" required placeholder="September 12, 2026 Edition"/></label><label>Issue date<input name="issue_date" type="date" required/></label><label className="md:col-span-2">Description<textarea name="description" rows={3}/></label><label>PDF edition<input name="pdf" type="file" accept="application/pdf"/></label><label>Cover image<input name="cover" type="file" accept="image/*"/></label><label>Existing PDF URL<input name="pdf_url" placeholder="Optional if not uploading"/></label><label>Flipbook URL<input name="flipbook_url" placeholder="Optional external reader"/></label><label>Status<select name="status" defaultValue="published"><option value="draft">Draft</option><option value="published">Published</option></select></label><label className="flex items-center gap-2"><input name="featured" value="true" type="checkbox"/> Feature this edition</label><button disabled={busy} className="hgn-btn-primary inline-flex items-center justify-center gap-2 md:col-span-2"><Upload size={17}/>{busy?'Uploading…':'Add edition to newsstand'}</button></form><section className="space-y-3">{items.map(i=><article key={i.id} className="grid gap-4 rounded-2xl border bg-white p-5 shadow-sm md:grid-cols-[90px_1fr_auto]"><div className="flex h-28 items-center justify-center overflow-hidden rounded border bg-stone-100">{i.cover_image_url?<img src={i.cover_image_url} alt="" className="h-full w-full object-cover"/>:<Archive/>}</div><div><h2 className="text-xl font-black">{i.title}</h2><p className="text-sm text-slate-500">{i.issue_date} · {i.status}</p><div className="mt-3 flex flex-wrap gap-2"><button onClick={()=>void patch(i.id,{status:i.status==='published'?'draft':'published'})} className="rounded-full border px-3 py-1.5 text-sm font-bold">{i.status==='published'?'Unpublish':'Publish'}</button><button onClick={()=>void patch(i.id,{featured:!i.featured})} className="rounded-full border px-3 py-1.5 text-sm font-bold">{i.featured?'Unfeature':'Feature'}</button>{i.pdf_url&&<a href={i.pdf_url} target="_blank" rel="noreferrer" className="rounded-full border px-3 py-1.5 text-sm font-bold">Open PDF</a>}</div></div><button onClick={()=>void del(i.id)} className="self-start rounded-full border p-2 text-red-700"><Trash2 size={16}/></button></article>)}</section></main>}
