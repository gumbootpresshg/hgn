"use client";
import { useState } from "react";
import { supabase } from "@/lib/supabase";
const towns=["Masset","Old Massett","Port Clements","Tlell","Skidegate","Daajing Giids","Sandspit","Other"];
export default function SuggestStoryPage(){const[form,setForm]=useState({name:"",email:"",phone:"",town:"",title:"",details:""});const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMsg("Sending...");const{error}=await supabase.from("story_tips").insert({...form,status:"new"});setMsg(error?error.message:"Thanks — your story idea was sent to HGN.");}return <main className="mx-auto max-w-4xl px-4 py-10"><p className="text-sm font-black uppercase tracking-widest text-hgnBlue">News tip</p><h1 className="mt-2 text-5xl font-black text-hgnNavy">Suggest a story</h1><p className="mt-4 text-lg leading-8 text-slate-700">A simple tip form for council issues, sports, community people, businesses, photos, concerns, road/ferry notes or anything worth looking into.</p><form onSubmit={submit} className="hgn-card mt-8 grid gap-4 p-6 md:grid-cols-2"><input className="hgn-input" placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="hgn-input" placeholder="Email" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="hgn-input" placeholder="Phone" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><select className="hgn-input" value={form.town} onChange={e=>setForm({...form,town:e.target.value})}><option value="">Town / area</option>{towns.map(t=><option key={t}>{t}</option>)}</select><input className="hgn-input md:col-span-2" placeholder="Story idea headline" required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><textarea className="hgn-input min-h-44 md:col-span-2" placeholder="What should we know?" required value={form.details} onChange={e=>setForm({...form,details:e.target.value})}/><button className="hgn-btn-primary md:col-span-2">Send story idea</button>{msg&&<p className="font-semibold text-hgnNavy md:col-span-2">{msg}</p>}</form></main>}
