# HGN v249 — Homepage Event Time Fix

Removed event times from homepage/sidebar Upcoming Events only. The `/events` page was not changed and should still show times.

Patched files:
- `src/app/page.tsx`

No SQL required.
