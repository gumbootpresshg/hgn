import { supabase } from "@/lib/supabase";
export const dynamic = "force-dynamic";
export default async function SeoDeskPage(){
 const {data:articles}=await supabase.from("articles").select("id,title,slug,seo_title,seo_description,image_url,published_at,google_news_ready").eq("status","published").order("published_at",{ascending:false}).limit(50);
 return <main className="mx-auto max-w-6xl px-4 py-10"><p className="text-sm font-black uppercase tracking-wide text-hgnBlue">Google News</p><h1 className="mt-2 text-4xl font-black text-hgnNavy">SEO Readiness Desk</h1><p className="mt-3 max-w-3xl text-slate-600">Review recent stories for headline, description, image and Google News readiness.</p><section className="mt-8 grid gap-4">{(articles||[]).map((a:any)=>{const missing=[!a.slug&&"slug",!a.seo_description&&"seo description",!a.image_url&&"image",!a.published_at&&"published date"].filter(Boolean);return <article key={a.id} className="rounded-2xl border bg-white p-5 shadow-sm"><span className={missing.length?"rounded-full bg-yellow-50 px-3 py-1 text-xs font-black text-yellow-800":"rounded-full bg-green-50 px-3 py-1 text-xs font-black text-green-800"}>{missing.length?`Needs: ${missing.join(", ")}`:"Looks ready"}</span><h2 className="mt-3 text-xl font-black text-slate-950">{a.title}</h2><p className="mt-2 text-sm text-slate-600">/{a.slug}</p></article>})}</section></main>
}