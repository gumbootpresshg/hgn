# Local validation status

`npm run typecheck` passed in the build workspace.

The hosted Next/Turbopack build process did not return a final result before the sandbox runner ended it. Run `npm.cmd run build` on the HGN deployment computer; this package is therefore marked `LOCAL-VALIDATION`.
